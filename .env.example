# 本機開發用。複製成 .env 後以 `uv run --env-file .env berth serve` 啟動。
# 容器部署的變數（PUID / PGID / UMASK / TZ / COMPOSE_PROFILES）在 deploy/.env.example。

# 資料庫、設定與 log 的位置。容器內是 /config；本機開發指到 repo 外的可寫目錄。
CONFIG_ROOT=./.local/config

# 媒體根。容器內是 /data；incomplete / complete 與媒體庫路徑都在它底下（brief §4.1）。
DATA_ROOT=./.local/data

# 對外的唯一 port。
PORT=8383

# 前端 build 產物。留空即用 repo 佈局的 web/dist；容器 image 由 Dockerfile 覆寫。
# WEB_ROOT=./web/dist
