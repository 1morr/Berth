# 本機開發用。複製成 .env 後以 `uv run --env-file .env berth serve` 啟動。
# 容器部署的變數（PUID / PGID / UMASK / TZ / COMPOSE_PROFILES）在 deploy/.env.example。

# 資料庫、設定與 log 的位置。容器內是 /config；本機開發指到 repo 外的可寫目錄。
CONFIG_ROOT=./.local/config

# 媒體根。容器內是 /data；incomplete / complete 與媒體庫路徑都在它底下（brief §4.1）。
DATA_ROOT=./.local/data

# 對外的唯一 port。
PORT=8383

# 前端 build 產物。留空即用 repo 佈局的 web/dist；容器 image 由 Dockerfile 覆寫。
# WEB_ROOT=./web/dist

# 其他服務唯讀掛進來的設定目錄（容器內是 /ext）。精靈從 ${EXT_ROOT}/prowlarr/config.xml
# 讀 Prowlarr 自動產生的 API key，使用者就不必自己去抄（plan §9.2）。
# EXT_ROOT=./.local/ext

# 用 PROWLARR__AUTH__APIKEY 部署 Prowlarr 的人，把同一個值也給 Berth，
# 就不需要上面那個唯讀掛載。有值時蓋過 config.xml。
# PROWLARR__AUTH__APIKEY=

# 只給**開發時的演練、實驗腳本與 e2e** 用的 TMDB 憑證（v3 API key 或 v4 read access token）：
# `scripts/fake_setup_server.py --scenario discover`、`scripts/experiments/*`、
# 錄 benchmark 快照的 `scripts/record_tmdb_snapshots.py`，以及 `pytest -m e2e`（它拿這把走精靈第 6 步，
# README〈e2e〉；CI 用同名的 repo secret）。
# **Berth 本身不讀它** —— 產品的唯一來源是 `settings.services.tmdb.api_key`，由精靈第 6 步
# 寫進資料庫（票 02b：不內建任何 provider 的 key）。
# TMDB_API_KEY=
