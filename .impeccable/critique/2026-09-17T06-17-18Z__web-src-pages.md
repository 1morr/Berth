---
target: M1 new pages
total_score: 26
max_score: 40
na_heuristics:
p0_count: 0
p1_count: 3
target_identity: "file:C:\\Users\\Roxy\\orca\\projects\\MediaServer\\web\\src\\pages"
timestamp: 2026-09-17T06-17-18Z
slug: web-src-pages
---
# /impeccable critique — Berth M1 新頁面（2026-09-17，票 15）

Method: dual-agent（A：設計審查，opus · B：檢測器 + 瀏覽器量測，sonnet），兩者互相隔離。
範圍：探索 `/`、Media 詳情 `/media/:id`（含搜尋結果與送單）、下載 `/jobs`、媒體庫 `/library/:routeSlug`、
Route 設定 `/settings/routes`。真環境 8383（e2e 那一套：三部不經人工入庫的作品）、fake `submit` 8484、
fake `inventory` 8485；1280 / 390、深淺兩主題、zh-Hant 與 EN。B 的機械證據由主對話另開瀏覽器逐條核對。

## Design Health Score — 26 / 40（Acceptable）

| # | 啟發式 | 分 | 關鍵問題 |
|---|---|---|---|
| 1 | 系統狀態可見 | 3 | medium audit 在摘要層看不見；沒接索引站要按了搜尋才知道；送單成功沒有 live 宣告 |
| 2 | 貼近真實世界 | 2 | EN 介面的標題與簡介是 zh-TW；`Moored`、`Already so`、`episodes in` |
| 3 | 使用者控制與自由 | 2 | 確認面板 Esc 不關、焦點掉回 body；1213 集季表只能捲回頂端收起 |
| 4 | 一致性與標準 | 3 | `·` 與 `Dot` 兩種；信心兩套詞；`client_removed` 紅 / 中性不一 |
| 5 | 錯誤預防 | 3 | 凍結資料夾名先給看、被引用 Route 不給刪；沒選 Route 也按得下確認送單 |
| 6 | 辨識優於回憶 | 2 | 確認面板不重述 Route；可展開的列看不出能展開 |
| 7 | 彈性與效率 | 2 | 沒有 skip link；缺集不能一鍵搜；頁首不 sticky |
| 8 | 美學與極簡 | 3 | 路徑印 39 次、「對得上 · 已收錄」39 次 |
| 9 | 診斷與復原 | 3 | 錯誤句尾懸冒號、改選後不清；`medium_not_allowed` 指向不存在的設定 |
| 10 | 說明與文件 | 3 | 情境內說明夠，沒有文件出口 |

## Design Specificity Verdict

系統層級專屬（纜繩、黃漆主動作、凍結資料夾名、刪不得的說明），頁面構圖套路（海報牆、整寬區塊疊）。
最有價值的資訊（待確認、缺集）構圖最弱。CLI 檢測器 0 findings；頁內 overlay 128 條，其中 `text-occlusion`
是誤報（收合 `<details>` 的內容未渲染，`checkVisibility()` false），`<summary>` 包連結屬實，其餘併入下面。

## Priority Issues

1. [P1] 390px 破版三處：`/jobs` 頁面層級橫向捲動 67px（無空格發佈名撐開 flex/grid 子項，`break-words` 無效；同根因在 SearchResults / JobPlan / FilesPanel / JobTimeline）；季列季名寬度 0 與集數重疊（SeasonList.tsx:37）；集表「入庫」欄在窄版畫面外。→ /impeccable adapt
2. [P1] medium 自動入庫的 audit 在 Job 列、媒體庫卡片與篩選都看不見（Frieren 11 個待確認只在展開 7,876px 後的弱字）。→ /impeccable clarify
3. [P1] 送單流程：焦點掉回 body、Esc 不關、紅字懸冒號且改選不清、不重述 Route、成功無 aria-live；ConfirmAction 同樣焦點問題。→ /impeccable harden
4. [P2] 結構性無障礙：探索與 Route 設定無 h1、詳情 h1→h3、頁首無 `<nav>` 與 skip link、JobRow `<summary>` 包連結、語言鍵 23px 間距 1px、MediaWall `aria-label` 掛 `<p>`、MediaTile `·`。→ /impeccable audit
5. [P2] 詳情頁動作沉底（搜尋在 1280 的 y=984、390 的第三屏）、全綠纜繩佔 311px；可展開列無提示、Frieren 展開 7,876px。→ /impeccable distill + layout

## Persona Red Flags

- Alex：無 `/` 快捷鍵、不能只看缺集、缺集不能一鍵搜、每次重選 Route、手機探索頁 14,260px。
- Sam：焦點丟失、無 skip link、summary 包連結、探索頁無 h1、送單成功無宣告。
- Casey：頁首 4 行不 sticky、`/jobs` 橫向捲動、送單鍵在第三屏且與 Route 相隔 635px、篩選方塊約 30px。

## Minor Observations

- FilesPanel 把 S00 特典標「正片」；`medium_not_allowed` 文案指向不存在的開關；EN `Library paths` 撞名；Route 名放 `.label` 被大寫。
- 未改過的表單黃色「儲存」亮著；全部佔用時「建立並檢查」仍是主動作。
- 語言鍵選中用 `assigned`，與 Role Is Not A State Rule 矛盾。
- SeasonList 收起仍渲染整張表；絕對編號欄位置與 shape §8 不符。
- 卡片「還在掃描」不自動更新。
- fake_setup_server.py 把 demo torrent 網址寫死 8484。

## Questions to Consider

- 顯示用標題跟 UI 語言走，還是獨立的片庫語言？
- 搜尋為什麼從作品名開始，而不是從缺口開始？
- 詳情頁版面在票 15 重排，還是交給 M1.5 的 shape？
