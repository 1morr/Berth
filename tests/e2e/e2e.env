# e2e 的 compose 變數（tests/e2e/compose.yml）。CONFIG_ROOT 不在這裡：它是宿主上的一個空目錄，
# 由呼叫的人給（CI 是 runner.temp），compose 找不到時會直接說。
COMPOSE_PROFILES=jellyfin,qbittorrent,prowlarr
DATA_ROOT=berth-e2e-data
PUID=1000
PGID=1000
UMASK=022
TZ=Etc/UTC
